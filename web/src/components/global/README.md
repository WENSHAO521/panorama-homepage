# components/global

Reserved for site-wide chrome shared by every page: the eventual global
`<Header>` and `<Footer>` composites (each built from `navigation/` and
`ui/` primitives). Not built in Version 1 — see project brief §23/§32
("Do not build the full header ... or footer yet") and
`docs/rebuild/ROADMAP.md` Round 4 (Global Shell).
