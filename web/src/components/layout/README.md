# components/layout

Reserved for structural/layout composites shared across page types
(e.g. a two-column article shell, a sidebar/TOC pattern) that are more
than a single `ui/` primitive but not a full page `layouts/*.astro`.
Empty in Version 1 — nothing yet needs this level between `ui/` and
`layouts/`.
